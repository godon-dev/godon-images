name: "release godon-causal image"

on:
  push:
    tags:
      - 'godon-causal-*'

permissions:
  contents: write
  packages: write

jobs:
  build-and-release-image:
    uses: ./.github/workflows/release-image.yml
    secrets: inherit
