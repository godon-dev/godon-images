name: "release godon-bench-generic image"

on:
  push:
    tags:
      - 'godon-bench-generic-*'

permissions:
  contents: write
  packages: write

jobs:
  build-and-release-image:
    uses: ./.github/workflows/release-image.yml
    secrets: inherit
