name: "release godon-bench-microgrid image"

on:
  push:
    tags:
      - 'godon-bench-microgrid-*'

permissions:
  contents: write
  packages: write

jobs:
  build-and-release-image:
    uses: ./.github/workflows/release-image.yml
    secrets: inherit
