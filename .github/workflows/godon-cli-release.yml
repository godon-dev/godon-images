
name: "release godon-cli image"

on:
  push:
    tags:
      - 'godon-cli-*'

permissions:
  contents: write
  packages: write

jobs:
  build-and-release-image:
    uses: ./.github/workflows/release-image.yml
    secrets: inherit