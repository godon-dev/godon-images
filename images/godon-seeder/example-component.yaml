name: controller
target: f/controller

scripts:
  - pattern: "*.py"
    settings:
      is_perennial: true
      description: "Controller Python scripts"
      timeout: "60s"

flows:
  - pattern: "*.json"
    settings:
      annotation: "Controller flows"